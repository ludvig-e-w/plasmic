## 2026-04-10

- Fixed a WAB editor build failure caused by a missing shared copilot tool-definition module import.
- Updated the local git remote to the user's GitHub repository and configured SSH agent/keychain authentication for passwordless pushes.
- Added a short local-auth note for Docker dev users so they can log in with the seeded accounts instead of waiting on verification email.
